import { useEffect,useState } from 'react';import { Search } from 'lucide-react';import { api } from '../services/api';import type { Automation } from '../types';
export default function AutomationPage(){const [items,setItems]=useState<Automation[]>([]);const [q,setQ]=useState('');const load=async(term='')=>setItems(await api.automations(term));useEffect(()=>{load()},[]);return <section className="page"><div className="page-heading"><p className="eyebrow">DISCOVER EXISTING CAPABILITIES</p><h1>Automation Catalog</h1><p>Find automations already available before creating a new solution.</p></div><form className="searchbar" onSubmit={e=>{e.preventDefault();load(q)}}><Search size={18}/><label className="sr-only" htmlFor="auto-search">Search automations</label><input id="auto-search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search Excel, reports, browser automation…"/><button>Search</button></form><div className="card-grid">{items.map(x=><article className="catalog-card" key={x.id}><div className="status-row"><span className="pill">{x.status}</span><span>{x.technology}</span></div><h2>{x.name}</h2><p>{x.short_description}</p><dl><div><dt>Business function</dt><dd>{x.business_function||'—'}</dd></div><div><dt>Capabilities</dt><dd>{x.capabilities||'—'}</dd></div><div><dt>Owner</dt><dd>{x.owner||'—'}</dd></div></dl></article>)}</div></section>}
